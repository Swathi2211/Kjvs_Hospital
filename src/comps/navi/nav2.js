import React, { useRef } from "react";

function Navi2({ jhgfRef, swatRef }) {
    return (
      <>
        <p ref={jhgfRef} style={{ padding: "500px", border: "1px solid black" }}>
          anbv
        </p>
        <p ref={swatRef} style={{ padding: "500px", border: "1px solid black" }}>
          iuyt
        </p>
      </>
    );
  }

  export default Navi2;